# Well Document Extraction

Local pipeline for extracting, normalizing, validating, and tracing
well-equipment data from vendor manuals, P&IDs, scanned forms, and handwritten annotations.

## Setup

```bash
source .venv/bin/activate
pip install -r requirements.txt
```
